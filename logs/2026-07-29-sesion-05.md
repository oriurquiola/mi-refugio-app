# Log · Sesión 05 — 2026-07-29

## Qué se hizo
- **Respiración guiada a 3 ciclos (57 s)** con corte de ciclo visible (`src/screens/BreathingExercise.tsx`). Cierra la contradicción con la etiqueta "60 seg" de la card. `BREATHING_CYCLES = 3`; el `totalSeconds` viejo pasó a `cycleSeconds` y el temporizador ubica la fase por módulo; marcador y escala duran un ciclo y se repiten (`repeat`) en vez de estirarse. Ver `decisions/0011`.
- **Señal de fin de ciclo, tres capas:** el marcador vuelve arriba, pulsa un anillo lavanda (0,8 s, escala 1 → 1.12) y avanza un indicador de 3 puntos + "Respiración N de 3". El `aria-live` se movió del párrafo de instrucción a esa línea.
- **Copy:** `t1.steps[3]` pasó a "Repite el ciclo 3 veces en total." (decía "3 veces más", que implicaba 4 ciclos). No se renderiza para `t1` desde `0008`, pero contradecía a la guía.
- **Push directo a `main`**: commit `1455fb0` (`d43e4d9..1455fb0`), 6 archivos. `.claude/settings.local.json` quedó fuera a propósito.

## Decisiones
- `0011` — 3 ciclos, ritmo continuo, indicador de puntos + texto. **Actualiza el punto "Duración" de `0008`** (no lo reemplaza). Se descartó la micro-pausa de 1,5 s entre ciclos que daba 60 s exactos: cortar el 4-7-8 dos veces pesa más que cuadrar el número.
- El índice de `decisions/README.md` estaba sin `0010`; se agregó junto con `0011`.

## Hallazgos clave
- **Medir > mirar** para animaciones: muestreo cada 50 ms a `window.__b` dio el corte exacto (**19,00 s**), la curva del pulso (**0,86 → 0** en ~0,75 s), la ausencia de salto en el empalme exhala→inhala (escala 0,52, mayor salto entre muestras 0,011) y el fin a **~57 s**. Ver `gotchas/README.md` G9.
- **Para ver** una animación de < 1 s hay que congelar el frame reescribiendo `style` con `important` cada 16 ms (motion y React lo pisan en cada render). Así se comprobó que con `scale: 1.06` el pulso quedaba montado sobre el anillo de arcos y con **1.12** se despega. G9.
- **Falsos positivos en la consola:** dos errores de React sobre el array de dependencias eran artefacto del hot-reload entre dos ediciones; el buffer del preview **no se limpia al recargar**. Se confirmó con una marca (`console.log`) + reload + flujo completo. G8.

## Estado final
- Todo en `origin/main` (`1455fb0`). `npm run lint` OK, logs del server sin errores, sin scroll ni recorte a 375×667.
- El camino `prefers-reduced-motion` quedó **revisado en código, no ejercitado en vivo**: las herramientas de preview no exponen ese toggle.

## Próximo paso
- Verificar reduced motion en un navegador real. Sigue pendiente salir del modo demo (`DEMO_PINNED_TECHNIQUE_ID` → `null`) y el copy "Basada en lo que sientes ahora".
