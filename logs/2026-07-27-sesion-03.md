# Log · Sesión 03 — 2026-07-27

## Qué se hizo
- **Recomendación real según síntomas**: modelo `matchesSymptomIds` en `types.ts`/`data.ts` (cubre los 18 síntomas) + score/orden en `Recommendations.tsx`. "Técnicas para ti" (coincidentes, por relevancia) + "Otras técnicas que pueden ayudar" (resto) + chips "Para lo que sientes ahora". Fallback: nunca vacío. Ver `decisions/0005`.
- **Marco mobile en desktop**: `App.tsx` envuelve la app en columna centrada `max-w-[440px]` con `translateZ(0)` para contener los `position: fixed`. Ver `decisions/0006`. Reemplaza gotcha G5.
- **Eliminado** el footer "Mi historial" del Home.
- **Git/PR**: rama `remove-mi-historial-home` → PR #1 (creado vía API, `gh` no instalado). A pedido de la usuaria, el cambio se aplicó **directo a `main`** (fast-forward `3bcac73..aef3b60`, push). PR #1 **cerrado** y rama eliminada (local + remota).

## Decisiones
- `0005` recomendación por síntomas (reemplaza alcance MVP de `0002`); `0006` marco mobile en desktop.

## Hallazgos clave
- `git diff HEAD` mostraba solo `Home.tsx`: **el resto de la sesión ya estaba en `origin/main`** → el repo auto-sincroniza a GitHub (flujo tipo AI Studio).
- `gh` NO está instalado en esta máquina; crear PRs requiere API + credencial del push. No puedes **aprobar tu propio PR** en GitHub (solo Comment/Request changes).
- `npm run lint` quedó **limpio** (los 3 errores tsc de Home/Processing ya no aparecen).

## Estado final
- Todo en `origin/main` (commit `aef3b60`). Rama única: `main`. Ver `state/current.md` (Sesión 03).

## Próximo paso
- Decidir uso de `mockHistory` en Home; afinar mapeo `matchesSymptomIds`; definir fallback/destino del chat de Quédate.
